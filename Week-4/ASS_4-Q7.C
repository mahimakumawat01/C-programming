#include <stdio.h>
int main ()
{
    // let a be the distance in between two cities
    int a,b;
    printf("entre distance in km is");
    scanf("%d",&a);
    b=a*100000;
    printf("distance in cm is %d",b);
    return 0;
}