#include <stdio.h>
int main()
{
    // write a program to calculate circumferencne of cirlce
    // let r be the radius of circle
    int r;
    printf("entre radius");
    scanf("%d",&r);
    int pi=3.14;
    printf("circumfrence of circle %d",2*pi*r);
    return 0;
}